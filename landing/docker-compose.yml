services:
  landing:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: puppydataserver-landing
    restart: unless-stopped
    ports:
      - "3000:3000"
    volumes:
      - ./stats.json:/app/stats.json
      - ./logs:/app/logs
    env_file:
      - .env
